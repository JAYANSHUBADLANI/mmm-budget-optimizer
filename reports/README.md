# reports

Generated output. This folder is gitignored.

`tables/` holds every result the pipeline produces as CSV or JSON, `figures/` holds the
charts. Anything currently present came from the simulated stand in dataset and is a wiring
demonstration, not a result. Rerun the phase scripts on the real data before quoting
anything from here.
